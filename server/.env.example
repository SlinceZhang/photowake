# Copy this file to .env (or provide the same variables via your process manager) before starting the NestJS server.

# The port the NestJS HTTP server listens on. Docker and nginx expect this to stay 3080.
PORT=3080
